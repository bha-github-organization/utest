<settings>
  <servers>
    <!-- Beacon Hill : begin -->
    <server>
      <id>bha-maven-repo-snapshot</id>
      <username>${env.BHA_REPO_USERNAME}</username>
      <password>${env.BHA_REPO_PASSWORD}</password>
    </server>
    <!-- Beacon Hill : end -->

    <server>
      <id>central</id>
      <username>${env.MAVEN_USERNAME}</username>
      <password>${env.MAVEN_PASSWORD}</password>
    </server>
  </servers>
</settings>
